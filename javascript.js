let gameBoard ={
    grid: [
    [0, 0, 0],
    [0, 0, 0],
    [0, 0, 0]
    ]
};

const playerFactory = (name, symbol) => {
    return {name, symbol};
}

let gameFlow = {
    startGame: function(){

    },
    endGame: function(){

    }
};
